# S1.4.1.3 â€” NormalizaciÃ³n de la suite de pruebas

**Fase:** S1.4 â€” ReconstrucciÃ³n funcional sobre baseline saneada
**Subfase:** S1.4.1 â€” Baseline funcional e integridad arquitectÃ³nica
**Componente:** Suite de pruebas
**Estado:** CERRADO / VALIDADO

## 1. Objetivo

Normalizar la suite de pruebas despuÃ©s del saneamiento y de la normalizaciÃ³n estructural del ERP Core, eliminando pruebas obsoletas o ficticias, corrigiendo dependencias del entorno de testing y fortaleciendo la cobertura de componentes crÃ­ticos.

El propÃ³sito fue asegurar que una prueba aprobada represente comportamiento real del baseline y no Ãºnicamente boilerplate heredado.

## 2. PrecondiciÃ³n heredada de S1.4.1.1

La reubicaciÃ³n de:

`app/Core/ValueObjects/ModuleIdTest.php`

a:

`tests/Unit/Core/ValueObjects/ModuleIdTest.php`

fue realizada durante **S1.4.1.1 â€” Integridad estructural del ERP Core**.

Se considera una precondiciÃ³n de esta subfase y no una acciÃ³n propia de S1.4.1.3.

## 3. NormalizaciÃ³n del entorno SQLite de pruebas

Durante la ejecuciÃ³n de la suite se detectÃ³ que el PHP CLI utilizado por el proyecto no tenÃ­a habilitado el driver SQLite requerido por el entorno de pruebas.

El PHP CLI activo correspondÃ­a a:

`C:\php84\php.ini`

Se habilitaron las extensiones necesarias:

- `pdo_sqlite`
- `sqlite3`

Con ello la suite pudo utilizar correctamente la base de datos de testing.

Este hallazgo se tratÃ³ como una dependencia del entorno local, no como un defecto de la arquitectura del ERP.

## 4. RestauraciÃ³n de password_reset_tokens

DespuÃ©s de habilitar SQLite, las pruebas de recuperaciÃ³n de contraseÃ±a revelaron que el baseline ya no contenÃ­a la tabla requerida por el password broker de Laravel.

Se normalizÃ³:

`database/migrations/0001_01_01_000001_create_users_table.php`

incorporando:

```php
Schema::create('password_reset_tokens', function (Blueprint $table) {
    $table->string('email')->primary();
    $table->string('token');
    $table->timestamp('created_at')->nullable();
});
```

Y en `down()`:

```php
Schema::dropIfExists('password_reset_tokens');
Schema::dropIfExists('users');
```

La tabla quedÃ³ alineada con la configuraciÃ³n del broker `users`.

Las cuatro pruebas de `PasswordResetTest` fueron posteriormente validadas.

## 5. EliminaciÃ³n de RegistrationTest

Se eliminÃ³:

`tests/Feature/Auth/RegistrationTest.php`

El registro pÃºblico habÃ­a sido eliminado intencionalmente durante el saneamiento previo del repositorio.

Mantener una prueba de registro pÃºblico habrÃ­a contradicho la baseline funcional vigente.

**DecisiÃ³n:** eliminar la prueba obsoleta en lugar de restaurar una funcionalidad que ya no forma parte del alcance.

## 6. EliminaciÃ³n de CreateUserTest

Se eliminÃ³:

`tests/Feature/CreateUserTest.php`

La prueba correspondÃ­a a comportamiento heredado que ya no representaba el flujo canÃ³nico de identidad del baseline saneado.

No se modificÃ³ el cÃ³digo productivo para satisfacer una prueba obsoleta.

## 7. NormalizaciÃ³n de Feature ExampleTest

La prueba genÃ©rica de ejemplo se reemplazÃ³ por una comprobaciÃ³n funcional del comportamiento real de la raÃ­z de la aplicaciÃ³n.

`tests/Feature/ExampleTest.php` quedÃ³ orientado a verificar que:

```text
GET /
    â†“
redirect
    â†“
dashboard
```

La prueba resultante valida que la ruta raÃ­z redirige correctamente hacia `route('dashboard')`.

## 8. EliminaciÃ³n de Unit ExampleTest

Se detectÃ³:

`tests/Unit/ExampleTest.php`

con una Ãºnica comprobaciÃ³n:

```php
$this->assertTrue(true);
```

Esta prueba siempre era exitosa independientemente del estado de la aplicaciÃ³n.

**DecisiÃ³n:** eliminarla como boilerplate sin valor de regresiÃ³n.

## 9. EliminaciÃ³n de archivo vacÃ­o en Providers

Se detectÃ³:

`tests/Unit/Core/Providers/ERPServiceProvider.php`

sin contenido funcional.

No constituÃ­a una prueba vÃ¡lida y se eliminÃ³.

La existencia del provider productivo no justifica conservar un archivo de test vacÃ­o.

## 10. ConservaciÃ³n de fixture utilizada

Se auditÃ³:

`tests/Fixtures/Templates/service.stub`

y se confirmÃ³ una referencia real desde:

`tests/Unit/Core/FileSystem/StubWriterTest.php`

Por lo tanto, la fixture se conservÃ³.

La normalizaciÃ³n se realizÃ³ por uso real, no por eliminaciÃ³n indiscriminada.

## 11. Fortalecimiento de ModuleBuilderTest

`tests/Unit/Core/Builders/ModuleBuilderTest.php` contenÃ­a originalmente una prueba placeholder:

```php
public function test_environment_is_ready(): void
{
    $this->assertTrue(true);
}
```

Se sustituyÃ³ por pruebas reales del comportamiento de `ModuleBuilder`.

La nueva suite verifica:

1. construcciÃ³n completa de la estructura de un mÃ³dulo;
2. generaciÃ³n de un manifiesto vÃ¡lido;
3. rechazo de un nombre de mÃ³dulo vacÃ­o;
4. prevenciÃ³n de sobrescritura accidental;
5. sobrescritura cuando existe autorizaciÃ³n explÃ­cita.

### Aislamiento

Las pruebas utilizan un directorio temporal bajo el directorio temporal del sistema.

No escriben sobre:

`app/Modules`

Esto permite validar la construcciÃ³n de mÃ³dulos sin contaminar la estructura real del proyecto.

### Dependencias

El test construye el grafo real necesario para `ModuleBuilder`, incluyendo:

- `Filesystem`;
- `DirectoryManager`;
- `FileWriter`;
- `JsonWriter`;
- `TemplateEngine`;
- `StubWriter`;
- `GitKeepGenerator`.

Los templates utilizados corresponden a:

`app/Core/Templates/module`

### Resultado dirigido

La ejecuciÃ³n especÃ­fica de `ModuleBuilderTest` terminÃ³ en:

```text
5 passed
17 assertions
```

## 12. AuditorÃ­a final del directorio tests

El inventario final quedÃ³ compuesto por:

```text
tests/
â”œâ”€â”€ TestCase.php
â”œâ”€â”€ Feature/
â”‚   â”œâ”€â”€ ExampleTest.php
â”‚   â”œâ”€â”€ ProfileTest.php
â”‚   â””â”€â”€ Auth/
â”‚       â”œâ”€â”€ AuthenticationTest.php
â”‚       â”œâ”€â”€ EmailVerificationTest.php
â”‚       â”œâ”€â”€ PasswordConfirmationTest.php
â”‚       â”œâ”€â”€ PasswordResetTest.php
â”‚       â””â”€â”€ PasswordUpdateTest.php
â”œâ”€â”€ Fixtures/
â”‚   â””â”€â”€ Templates/
â”‚       â””â”€â”€ service.stub
â””â”€â”€ Unit/
    â””â”€â”€ Core/
        â”œâ”€â”€ Builders/
        â”‚   â””â”€â”€ ModuleBuilderTest.php
        â”œâ”€â”€ FileSystem/
        â”‚   â”œâ”€â”€ DirectoryManagerTest.php
        â”‚   â”œâ”€â”€ FileWriterTest.php
        â”‚   â”œâ”€â”€ GitKeepGeneratorTest.php
        â”‚   â”œâ”€â”€ JsonWriterTest.php
        â”‚   â””â”€â”€ StubWriterTest.php
        â””â”€â”€ ValueObjects/
            â””â”€â”€ ModuleIdTest.php
```

La auditorÃ­a final confirmÃ³:

```text
Archivos vacÃ­os en tests       0
assertTrue(true) residual       0
```

## 13. Suite completa de cierre

DespuÃ©s de la normalizaciÃ³n se ejecutÃ³:

```powershell
php artisan test
```

Resultado canÃ³nico de S1.4.1.3:

```text
Tests:       61 passed
Assertions:  119
Duration:    4.53s
```

Este resultado sustituye como baseline de testing al resultado anterior de S1.4.1.2 (`58 passed / 104 assertions`).

El incremento no representa simplemente mÃ¡s tests: deriva principalmente de sustituir pruebas ficticias por cobertura funcional real.

## 14. ERP Core despuÃ©s de normalizar tests

Se ejecutÃ³:

```powershell
php artisan erp:status
```

Resultado:

```text
ERP Core                     0.1.0
Descubrimiento automÃ¡tico    Habilitado
MÃ³dulos descubiertos         1
MÃ³dulo                       Organizacion
VersiÃ³n                      0.1.0
Provider                     App\Modules\Organizacion\Providers\OrganizacionServiceProvider
```

La normalizaciÃ³n de tests no alterÃ³ el descubrimiento modular.

## 15. git diff --check

La comprobaciÃ³n final no reportÃ³ errores bloqueantes.

Se observaron Ãºnicamente advertencias de futura normalizaciÃ³n CRLF â†’ LF en archivos concretos.

Estas advertencias se clasificaron como no bloqueantes para el cierre de la subfase.

## 16. Criterios de aceptaciÃ³n

| Criterio | Resultado |
|---|---|
| SQLite disponible para tests | VALIDADO |
| `password_reset_tokens` restaurada | VALIDADO |
| Password reset funcional | VALIDADO |
| Registro pÃºblico no reintroducido | VALIDADO |
| `RegistrationTest` obsoleto eliminado | VALIDADO |
| `CreateUserTest` obsoleto eliminado | VALIDADO |
| Feature ExampleTest con comportamiento real | VALIDADO |
| Unit ExampleTest boilerplate eliminado | VALIDADO |
| Archivo de test vacÃ­o eliminado | VALIDADO |
| Fixture `service.stub` con uso real conservada | VALIDADO |
| `ModuleBuilderTest` fortalecido | 5 PASSED / 17 ASSERTIONS |
| Archivos vacÃ­os en `tests/` | 0 |
| `assertTrue(true)` residual | 0 |
| Suite completa | 61 PASSED / 119 ASSERTIONS |
| ERP Core post-normalizaciÃ³n | VALIDADO |
| `git diff --check` | SIN ERRORES BLOQUEANTES |

## 17. Decisiones de testing

1. Una prueba debe representar comportamiento real y producir valor de regresiÃ³n.
2. No se modifica cÃ³digo productivo para satisfacer pruebas heredadas que contradicen la baseline.
3. Las pruebas de generaciÃ³n de mÃ³dulos deben ejecutarse en directorios temporales.
4. Las fixtures con referencias reales se conservan.
5. Los tests vacÃ­os, triviales o desalineados con el producto se eliminan.
6. La suite de pruebas debe ser coherente con las decisiones funcionales vigentes, incluida la ausencia de registro pÃºblico.
7. Los defectos del entorno de testing deben corregirse en el entorno cuando el cÃ³digo productivo no es la causa.

## 18. RelaciÃ³n con S1.4.1.4

Con la suite normalizada, S1.4.1.4 pudo utilizar:

```text
61 passed / 119 assertions
```

como criterio de aceptaciÃ³n durante la reconstrucciÃ³n completa del baseline.

Esto permitiÃ³ comprobar posteriormente que un `migrate:fresh --seed` no alteraba el comportamiento esperado del sistema.

## 19. Cierre

La suite dejÃ³ de contener pruebas ficticias, archivos vacÃ­os y escenarios funcionales obsoletos.

Se restaurÃ³ la infraestructura requerida para password reset, se fortaleciÃ³ `ModuleBuilderTest` con comportamiento real y se obtuvo una suite completa coherente con el baseline saneado.

```text
S1.4.1.3 â€” NormalizaciÃ³n de la suite de pruebas
ESTADO: CERRADO / VALIDADO

Tests:       61 passed
Assertions:  119
```

La suite quedÃ³ apta para actuar como criterio de aceptaciÃ³n de **S1.4.1.4 â€” ValidaciÃ³n del baseline funcional**.
