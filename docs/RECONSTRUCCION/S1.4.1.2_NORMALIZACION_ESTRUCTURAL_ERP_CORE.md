# S1.4.1.2 â€” NormalizaciÃ³n estructural del ERP Core

**Fase:** S1.4 â€” ReconstrucciÃ³n funcional sobre baseline saneada
**Subfase:** S1.4.1 â€” Baseline funcional e integridad arquitectÃ³nica
**Componente:** ERP Core
**Estado:** CERRADO / VALIDADO

## 1. Objetivo

Normalizar la estructura interna del ERP Core despuÃ©s de que S1.4.1.1 confirmara su integridad y eliminara los residuos estructurales iniciales.

Esta subfase se concentrÃ³ en consolidar implementaciones canÃ³nicas, reducir duplicidad, normalizar la inyecciÃ³n de dependencias, preservar la lÃ³gica funcional del kernel y depurar artefactos auxiliares sin uso.

No tuvo como objetivo agregar mÃ³dulos de negocio ni ampliar funcionalidad del ERP.

## 2. Precondiciones heredadas de S1.4.1.1

Las siguientes acciones ya habÃ­an sido ejecutadas y validadas en **S1.4.1.1 â€” Integridad estructural del ERP Core** y se consideran precondiciones de esta subfase, no trabajo atribuible a S1.4.1.2:

- eliminaciÃ³n de nueve archivos PHP vacÃ­os en `Exceptions` y `ValueObjects`;
- reubicaciÃ³n de `app/Core/ValueObjects/ModuleIdTest.php` a `tests/Unit/Core/ValueObjects/ModuleIdTest.php`;
- confirmaciÃ³n de ERP Core como infraestructura funcional;
- identificaciÃ³n de `TemplateEngine` como componente activo;
- preservaciÃ³n de los templates canÃ³nicos del mÃ³dulo.

Esta separaciÃ³n mantiene la trazabilidad entre auditorÃ­a estructural y normalizaciÃ³n interna.

## 3. ERPMakeModuleCommand

Se normalizÃ³:

`app/Console/Commands/ERPMakeModuleCommand.php`

El comando dejÃ³ de construir directamente la estructura de mÃ³dulos mediante operaciones de filesystem y pasÃ³ a depender de:

`App\Core\Contracts\ModuleBuilderContract`

La responsabilidad del comando quedÃ³ limitada a:

1. recibir el nombre del mÃ³dulo;
2. interpretar `--force`;
3. delegar la construcciÃ³n a `ModuleBuilderContract`;
4. gestionar el resultado o excepciÃ³n;
5. presentar al usuario nombre, ruta, provider y manifiesto.

Esto elimina duplicidad de responsabilidades entre la capa CLI y la infraestructura de construcciÃ³n.

### Contrato resultante

```text
erp:make-module
      â”‚
      â–¼
ModuleBuilderContract
      â”‚
      â–¼
ModuleBuilder
```

`ModuleBuilder` quedÃ³ establecido como implementaciÃ³n canÃ³nica para la creaciÃ³n de mÃ³dulos.

## 4. ModuleBuilder como implementaciÃ³n canÃ³nica

La revisiÃ³n confirmÃ³ que:

`app/Core/Builders/ModuleBuilder.php`

contiene la implementaciÃ³n completa necesaria para generar un mÃ³dulo ERP.

Entre sus responsabilidades se encuentran:

- normalizaciÃ³n del nombre mediante `Str::studly`;
- validaciÃ³n del nombre;
- prevenciÃ³n de sobrescritura accidental;
- creaciÃ³n de la estructura de directorios;
- generaciÃ³n de `.gitkeep` donde corresponde;
- generaciÃ³n de `module.json`;
- generaciÃ³n del Service Provider;
- generaciÃ³n de rutas web y API;
- generaciÃ³n de README;
- soporte explÃ­cito de sobrescritura.

El manifiesto generado contiene, entre otros:

```text
name
slug
version
description
provider
enabled
dependencies
```

La versiÃ³n inicial de un mÃ³dulo generado permanece en `0.1.0`.

## 5. ERPServiceProvider

Se normalizÃ³:

`app/Core/Providers/ERPServiceProvider.php`

El provider quedÃ³ como punto canÃ³nico para registrar las dependencias principales del ERP Core.

Se validaron los bindings:

```text
ERPKernelContract
    â†’ App\Core\Kernel\ERPKernel

ModuleBuilderContract
    â†’ App\Core\Builders\ModuleBuilder

FileWriterContract
    â†’ App\Core\FileSystem\FileWriter

JsonWriterContract
    â†’ App\Core\FileSystem\JsonWriter

StubWriterContract
    â†’ App\Core\FileSystem\StubWriter

GitKeepGeneratorContract
    â†’ App\Core\FileSystem\GitKeepGenerator
```

`ModuleBuilder` se construye mediante el contenedor con:

- `DirectoryManager`;
- `JsonWriterContract`;
- `StubWriterContract`;
- `GitKeepGeneratorContract`;
- `erp.modules_path`;
- `app/Core/Templates/module`.

El mÃ©todo `boot()` mantiene la delegaciÃ³n al `ERPKernelContract`.

## 6. ERPKernel

Se normalizÃ³:

`app/Core/Kernel/ERPKernel.php`

La intervenciÃ³n preservÃ³ la lÃ³gica funcional existente y corrigiÃ³ problemas de representaciÃ³n textual/mojibake y limpieza de formato.

La lÃ³gica preservada incluye:

- boot idempotente;
- lectura de configuraciÃ³n del ERP;
- autodiscovery de mÃ³dulos;
- lectura de `module.json`;
- validaciÃ³n de campos requeridos;
- interpretaciÃ³n de `enabled`;
- rechazo de mÃ³dulos duplicados;
- validaciÃ³n del provider;
- registro del provider;
- almacenamiento del manifiesto y ruta del mÃ³dulo descubierto.

No se introdujeron cambios de comportamiento de negocio durante esta normalizaciÃ³n.

## 7. TemplateEngine

Se confirmÃ³ nuevamente que:

`app/Core/Template/TemplateEngine.php`

es un componente activo.

Su responsabilidad incluye renderizar placeholders con sintaxis:

```text
{{Name}}
{{ Name }}
```

TambiÃ©n valida nombres de variables y participa en la escritura del resultado renderizado.

Tiene referencias funcionales desde `StubWriter` y desde las pruebas unitarias correspondientes.

**DecisiÃ³n:** conservar `app/Core/Template/`.

## 8. Templates canÃ³nicos

La ubicaciÃ³n canÃ³nica para generaciÃ³n de mÃ³dulos quedÃ³ definida en:

`app/Core/Templates/module/`

con:

- `api-routes.stub`
- `readme.stub`
- `service-provider.stub`
- `web-routes.stub`

Estos archivos son consumidos por `ModuleBuilder`.

## 9. EliminaciÃ³n de stubs duplicados de prueba

Se detectaron:

- `app/Core/Template/test.stub`
- `app/Core/Templates/test.stub`

Ambos archivos tenÃ­an el mismo contenido y el mismo SHA-256:

```text
637D857AEFCCDBB0891DD8D5016AF2FD26B66456E0D50A2080F12CFB35B5D3DD
```

No se encontraron referencias funcionales hacia ninguno.

**DecisiÃ³n:** eliminar ambos como artefactos de desarrollo duplicados.

La eliminaciÃ³n no afecta a `TemplateEngine.php` ni a los templates canÃ³nicos de `Templates/module`.

## 10. EliminaciÃ³n de directorios placeholder

Se revisaron:

- `app/Core/Events`
- `app/Core/Support`
- `app/Core/Traits`

Cada uno contenÃ­a Ãºnicamente un `.gitkeep` vacÃ­o y no se identificaron referencias de namespace que justificaran conservarlos.

Se eliminaron los `.gitkeep` y posteriormente los directorios vacÃ­os.

La estructura del Core dejÃ³ de representar Ã¡reas arquitectÃ³nicas todavÃ­a inexistentes.

## 11. Estado estructural resultante

DespuÃ©s de S1.4.1.2, las Ã¡reas estructurales relevantes del Core quedaron:

```text
app/Core/
â”œâ”€â”€ Builders/
â”œâ”€â”€ Contracts/
â”œâ”€â”€ Exceptions/
â”œâ”€â”€ FileSystem/
â”œâ”€â”€ Kernel/
â”œâ”€â”€ Providers/
â”œâ”€â”€ Template/
â”œâ”€â”€ Templates/
â””â”€â”€ ValueObjects/
```

No quedaron `.gitkeep` dentro de `app/Core`.

No quedaron archivos de longitud cero dentro de `app/Core`.

## 12. AuditorÃ­a UTF-8

Se realizÃ³ una auditorÃ­a estricta sobre archivos relevantes de:

- `app/Core`;
- `app/Console/Commands`.

La comprobaciÃ³n incluyÃ³ archivos PHP, stubs y documentaciÃ³n inspeccionada.

Resultado:

```text
UTF-8 invÃ¡lido       0
BOM                   0
Patrones mojibake     0
```

La visualizaciÃ³n anÃ³mala de la palabra `mÃ³dulo` en una salida de consola de `artisan list` se clasificÃ³ como un problema de representaciÃ³n/codepage de la terminal, no como corrupciÃ³n del archivo fuente, porque la auditorÃ­a de bytes confirmÃ³ UTF-8 vÃ¡lido.

## 13. ValidaciÃ³n tÃ©cnica

### Sintaxis y autoload

Los archivos modificados pasaron validaciones de sintaxis.

Composer regenerÃ³ correctamente el autoload optimizado.

En la validaciÃ³n de cierre de S1.4.1.2 se registrÃ³:

```text
Generated optimized autoload files containing 6729 classes
```

Las cifras de autoload pueden variar posteriormente al continuar la limpieza de tests y archivos residuales; este valor representa la evidencia del punto de cierre de esta subfase.

### ERP Core

`php artisan erp:status` confirmÃ³:

```text
ERP Core                     0.1.0
Descubrimiento automÃ¡tico    Habilitado
MÃ³dulos descubiertos         1
MÃ³dulo                       Organizacion
VersiÃ³n                      0.1.0
Provider                     App\Modules\Organizacion\Providers\OrganizacionServiceProvider
```

### Suite completa

La validaciÃ³n de cierre reportÃ³:

```text
58 passed
104 assertions
```

Esta cifra corresponde al punto de cierre de S1.4.1.2. La suite fue posteriormente normalizada en S1.4.1.3, por lo que el baseline final de S1.4.1 utiliza una cifra posterior.

### git diff --check

Solo se observaron advertencias de normalizaciÃ³n futura CRLF â†’ LF en archivos concretos.

No se identificaron errores bloqueantes de whitespace.

## 14. Criterios de aceptaciÃ³n

| Criterio | Resultado |
|---|---|
| `ERPMakeModuleCommand` delega en contrato | VALIDADO |
| `ModuleBuilder` establecido como implementaciÃ³n canÃ³nica | VALIDADO |
| Bindings del ERP Core normalizados | VALIDADO |
| `ERPServiceProvider` operativo | VALIDADO |
| `ERPKernel` preserva comportamiento | VALIDADO |
| `TemplateEngine` activo y conservado | VALIDADO |
| Templates de mÃ³dulo canÃ³nicos | VALIDADO |
| `test.stub` duplicados eliminados | VALIDADO |
| Directorios placeholder sin responsabilidad eliminados | VALIDADO |
| `.gitkeep` residuales en Core | 0 |
| Archivos cero bytes en Core | 0 |
| UTF-8 invÃ¡lido / BOM / mojibake fuente | 0 |
| Composer autoload | VALIDADO |
| ERP autodiscovery | VALIDADO |
| Suite en cierre de subfase | 58 PASSED / 104 ASSERTIONS |

## 15. Decisiones arquitectÃ³nicas

1. `ModuleBuilder` es la implementaciÃ³n canÃ³nica de generaciÃ³n de mÃ³dulos.
2. Los comandos Artisan deben delegar lÃ³gica de infraestructura a contratos/servicios del Core.
3. `ERPServiceProvider` concentra los bindings canÃ³nicos del ERP.
4. `ERPKernel` mantiene el descubrimiento modular como responsabilidad central.
5. `TemplateEngine` y `Templates/module` se conservan porque tienen responsabilidades diferentes y uso real.
6. No se mantienen directorios placeholder Ãºnicamente para anticipar funcionalidades futuras.
7. Los artefactos de prueba sin referencias dentro de cÃ³digo productivo se eliminan.
8. Los problemas visuales de terminal no se tratarÃ¡n como corrupciÃ³n de cÃ³digo sin evidencia a nivel de bytes.

## 16. Trazabilidad con S1.4.1.1

La eliminaciÃ³n de los nueve archivos PHP vacÃ­os y la reubicaciÃ³n de `ModuleIdTest` **no forman parte de las modificaciones realizadas en S1.4.1.2**.

Se mencionan Ãºnicamente como precondiciones heredadas de S1.4.1.1.

Esto corrige la atribuciÃ³n documental y mantiene una frontera clara:

```text
S1.4.1.1
Integridad / limpieza estructural inicial
        â”‚
        â–¼
S1.4.1.2
NormalizaciÃ³n interna del ERP Core
```

## 17. RelaciÃ³n con las siguientes subfases

Con la estructura interna normalizada, el siguiente paso fue:

**S1.4.1.3 â€” NormalizaciÃ³n de la suite de pruebas**

AllÃ­ se revisaron especÃ­ficamente pruebas obsoletas, boilerplate, archivos de test vacÃ­os y cobertura significativa de `ModuleBuilder`.

Posteriormente:

**S1.4.1.4 â€” ValidaciÃ³n del baseline funcional**

realizÃ³ la reconstrucciÃ³n completa de la base de datos y la validaciÃ³n integral del baseline.

## 18. Cierre

ERP Core quedÃ³ normalizado alrededor de responsabilidades canÃ³nicas y dependencias explÃ­citas.

Se redujo duplicidad, se eliminaron placeholders y stubs sin uso, se preservÃ³ la lÃ³gica funcional del kernel y se validÃ³ la integridad de codificaciÃ³n, autoload, descubrimiento modular y suite existente.

```text
S1.4.1.2 â€” NormalizaciÃ³n estructural del ERP Core
ESTADO: CERRADO / VALIDADO
```

El resultado establece una base estructural consistente para la normalizaciÃ³n de pruebas y la posterior validaciÃ³n integral del baseline.
