# S1.4.1 â€” Baseline funcional e integridad arquitectÃ³nica

**Fase:** S1.4 â€” ReconstrucciÃ³n funcional sobre baseline saneada
**Bloque:** S1.4.1 â€” Baseline funcional e integridad arquitectÃ³nica
**Rama de trabajo:** `rebuild/s1.4-base-funcional`
**Estado global:** CERRADO / VALIDADO

---

## 1. PropÃ³sito

S1.4.1 establece la primera baseline funcional posterior al saneamiento integral del repositorio.

Su propÃ³sito fue demostrar que la aplicaciÃ³n conserva Ãºnicamente una base tÃ©cnica coherente, reproducible y verificable antes de reconstruir las funcionalidades de identidad, autorizaciÃ³n y mÃ³dulos de negocio.

El bloque se desarrollÃ³ en cuatro subfases:

```text
S1.4.1
â”œâ”€â”€ S1.4.1.1  Integridad estructural del ERP Core
â”œâ”€â”€ S1.4.1.2  NormalizaciÃ³n estructural del ERP Core
â”œâ”€â”€ S1.4.1.3  NormalizaciÃ³n de la suite de pruebas
â””â”€â”€ S1.4.1.4  ValidaciÃ³n del baseline funcional
```

El cierre de S1.4.1 no representa la terminaciÃ³n del ERP. Representa la consolidaciÃ³n de una plataforma mÃ­nima y confiable sobre la cual continuar la reconstrucciÃ³n.

---

## 2. Principios aplicados

Durante S1.4.1 se mantuvieron los siguientes criterios:

1. No conservar cÃ³digo Ãºnicamente por pertenecer histÃ³ricamente al repositorio.
2. No eliminar componentes sin comprobar previamente si tienen uso real.
3. No mantener archivos, directorios o tests placeholder que simulen arquitectura inexistente.
4. No modificar cÃ³digo productivo para satisfacer pruebas obsoletas.
5. Mantener separadas las responsabilidades entre CLI, contratos, builders, filesystem y kernel.
6. Preservar una suite de pruebas que valide comportamiento real.
7. Exigir que la baseline pueda reconstruirse desde cero.
8. Diferenciar defectos del cÃ³digo de problemas del entorno local o del tooling.
9. Mantener trazabilidad documental por subfase antes de versionar el cierre.

---

# 3. S1.4.1.1 â€” Integridad estructural del ERP Core

**Estado:** CERRADO / VALIDADO

La primera subfase determinÃ³ si `app/Core` constituÃ­a infraestructura funcional o un residuo de desarrollos anteriores.

La auditorÃ­a confirmÃ³ que ERP Core contiene componentes activos para una arquitectura modular, entre ellos:

- `ERPKernel`;
- contratos;
- filesystem abstractions;
- `ModuleBuilder`;
- providers;
- `TemplateEngine`;
- templates;
- `ModuleId`.

Por ello, ERP Core fue conservado y sometido a saneamiento estructural.

### 3.1 Archivos PHP vacÃ­os eliminados

Se eliminaron nueve archivos sin implementaciÃ³n.

Exceptions:

```text
InvalidModuleIdException.php
InvalidModuleNameException.php
InvalidModuleVersionException.php
```

Value Objects:

```text
ModuleMetadata.php
ModuleName.php
ModuleNamespace.php
ModulePath.php
ModuleVersion.php
ProviderClass.php
```

Antes de eliminarlos se confirmÃ³ que tenÃ­an longitud cero y que no existÃ­an dependencias reales hacia esas clases.

### 3.2 ReubicaciÃ³n de ModuleIdTest

Se corrigiÃ³ una prueba localizada dentro del cÃ³digo productivo:

```text
app/Core/ValueObjects/ModuleIdTest.php
```

movida a:

```text
tests/Unit/Core/ValueObjects/ModuleIdTest.php
```

Esta acciÃ³n pertenece especÃ­ficamente a S1.4.1.1.

### 3.3 Resultado

ERP Core fue confirmado como infraestructura vÃ¡lida y quedÃ³ libre de los residuos estructurales iniciales.

Documento asociado:

`S1.4.1.1_INTEGRIDAD_ESTRUCTURAL_ERP_CORE.md`

---

# 4. S1.4.1.2 â€” NormalizaciÃ³n estructural del ERP Core

**Estado:** CERRADO / VALIDADO

Una vez demostrada la integridad del Core, la segunda subfase normalizÃ³ sus responsabilidades internas.

### 4.1 ERPMakeModuleCommand

`ERPMakeModuleCommand` dejÃ³ de implementar directamente operaciones de construcciÃ³n y pasÃ³ a depender de:

```text
ModuleBuilderContract
```

La arquitectura resultante quedÃ³:

```text
Artisan Command
      â”‚
      â–¼
ModuleBuilderContract
      â”‚
      â–¼
ModuleBuilder
```

El comando se limita a recibir argumentos, delegar la construcciÃ³n y presentar el resultado.

### 4.2 ModuleBuilder

`ModuleBuilder` quedÃ³ establecido como implementaciÃ³n canÃ³nica para generaciÃ³n de mÃ³dulos.

Entre sus responsabilidades se encuentran:

- normalizaciÃ³n del nombre;
- validaciÃ³n;
- creaciÃ³n de directorios;
- protecciÃ³n contra sobrescritura;
- generaciÃ³n del manifiesto;
- generaciÃ³n del provider;
- rutas web/API;
- README;
- soporte de sobrescritura explÃ­cita.

### 4.3 ERPServiceProvider

Se consolidaron los bindings canÃ³nicos:

```text
ERPKernelContract
    â†’ ERPKernel

ModuleBuilderContract
    â†’ ModuleBuilder

FileWriterContract
    â†’ FileWriter

JsonWriterContract
    â†’ JsonWriter

StubWriterContract
    â†’ StubWriter

GitKeepGeneratorContract
    â†’ GitKeepGenerator
```

### 4.4 ERPKernel

Se preservÃ³ su lÃ³gica funcional de:

- autodiscovery;
- lectura de `module.json`;
- validaciÃ³n de manifiestos;
- validaciÃ³n de providers;
- rechazo de duplicados;
- registro de mÃ³dulos habilitados.

La intervenciÃ³n se limitÃ³ a normalizaciÃ³n estructural y textual, sin introducir lÃ³gica de negocio.

### 4.5 Templates

Se conservaron:

```text
app/Core/Template/TemplateEngine.php
```

y los templates canÃ³nicos:

```text
app/Core/Templates/module/
â”œâ”€â”€ api-routes.stub
â”œâ”€â”€ readme.stub
â”œâ”€â”€ service-provider.stub
â””â”€â”€ web-routes.stub
```

Se eliminaron los `test.stub` duplicados y sin referencias.

### 4.6 Directorios placeholder

Se eliminaron directorios que solo contenÃ­an `.gitkeep`:

```text
Events
Support
Traits
```

Al cierre no quedaron `.gitkeep` ni archivos de longitud cero dentro de `app/Core`.

### 4.7 CodificaciÃ³n

La auditorÃ­a estricta confirmÃ³:

```text
UTF-8 invÃ¡lido       0
BOM                  0
Mojibake fuente      0
```

La representaciÃ³n incorrecta de caracteres observada ocasionalmente en la terminal fue clasificada como problema de codepage/renderizado, no corrupciÃ³n de fuente.

Documento asociado:

`S1.4.1.2_NORMALIZACION_ESTRUCTURAL_ERP_CORE.md`

---

# 5. S1.4.1.3 â€” NormalizaciÃ³n de la suite de pruebas

**Estado:** CERRADO / VALIDADO

La tercera subfase eliminÃ³ pruebas ficticias u obsoletas y convirtiÃ³ la suite en un criterio de regresiÃ³n significativo.

### 5.1 Entorno SQLite

El PHP CLI utilizaba:

```text
C:\php84\php.ini
```

Se habilitaron:

```text
pdo_sqlite
sqlite3
```

para permitir la ejecuciÃ³n correcta del entorno de testing.

### 5.2 password_reset_tokens

Se restaurÃ³ la tabla requerida por el password broker:

```text
password_reset_tokens
â”œâ”€â”€ email
â”œâ”€â”€ token
â””â”€â”€ created_at
```

La migraciÃ³n quedÃ³ incorporada a la baseline reproducible.

### 5.3 Tests obsoletos eliminados

Se eliminaron:

```text
tests/Feature/Auth/RegistrationTest.php
tests/Feature/CreateUserTest.php
tests/Unit/ExampleTest.php
tests/Unit/Core/Providers/ERPServiceProvider.php
```

El registro pÃºblico no fue reintroducido, ya que su eliminaciÃ³n era una decisiÃ³n funcional intencional.

### 5.4 ExampleTest

La prueba genÃ©rica fue sustituida por comportamiento real:

```text
GET /
   â†“
redirect
   â†“
dashboard
```

### 5.5 Fixture preservada

Se conservÃ³:

```text
tests/Fixtures/Templates/service.stub
```

porque existe una referencia funcional desde `StubWriterTest`.

### 5.6 ModuleBuilderTest

La prueba placeholder fue sustituida por cinco escenarios reales:

1. construcciÃ³n completa;
2. manifiesto vÃ¡lido;
3. rechazo de nombre vacÃ­o;
4. prevenciÃ³n de sobrescritura accidental;
5. sobrescritura explÃ­citamente autorizada.

Resultado dirigido:

```text
5 passed
17 assertions
```

Las pruebas trabajan en directorios temporales y no modifican `app/Modules`.

### 5.7 AuditorÃ­a final de tests

Resultado:

```text
Archivos vacÃ­os                0
assertTrue(true) residual      0
```

Suite completa:

```text
61 passed
119 assertions
```

Documento asociado:

`S1.4.1.3_NORMALIZACION_SUITE_DE_PRUEBAS.md`

---

# 6. S1.4.1.4 â€” ValidaciÃ³n del baseline funcional

**Estado:** CERRADO / VALIDADO

La cuarta subfase comprobÃ³ que la baseline completa puede reconstruirse desde cero y conservar exactamente su comportamiento esperado.

### 6.1 Entorno

```text
PHP CLI        8.4.5
php.ini        C:\php84\php.ini
Laravel        13.20.0
Composer       2.10.2
APP_ENV        local
Locale         es
```

Base de datos:

```text
Driver         mysql
Database       sembrando_vida
Host           127.0.0.1
Port           3307
```

### 6.2 Baseline de migraciones

```text
0001_01_01_000000_create_roles_table.php
0001_01_01_000001_create_users_table.php
0001_01_01_000002_create_cache_table.php
0001_01_01_000003_create_jobs_table.php
```

### 6.3 ReconstrucciÃ³n

Se ejecutÃ³:

```powershell
php artisan migrate:fresh --seed
```

Las cuatro migraciones y `RolesSeeder` se ejecutaron correctamente.

Roles resultantes:

```text
1  Administrador
2  Usuario
```

### 6.4 Tablas

La reconstrucciÃ³n produjo:

```text
cache
cache_locks
failed_jobs
job_batches
jobs
migrations
password_reset_tokens
roles
users
```

### 6.5 users

La tabla `users` quedÃ³ validada con 14 columnas:

```text
id
name
apellido_paterno
apellido_materno
curp
email
celular
email_verified_at
password
activo
role_id
remember_token
created_at
updated_at
```

Restricciones relevantes:

```text
email       UNIQUE
curp        UNIQUE
celular     UNIQUE
role_id     INDEX
```

Foreign key:

```text
users.role_id
      â”‚
      â–¼
roles.id

ON UPDATE RESTRICT
ON DELETE SET NULL
```

### 6.6 PHP intl

Para completar las herramientas de inspecciÃ³n del CLI se habilitÃ³:

```text
extension=intl
```

en:

```text
C:\php84\php.ini
```

La extensiÃ³n quedÃ³ validada con ICU 75.1.

Esta modificaciÃ³n pertenece al entorno local y no al repositorio Git.

### 6.7 db:show

`php artisan db:show` encontrÃ³ una incompatibilidad local relacionada con:

```text
performance_schema.session_status
```

Se clasificÃ³ como limitaciÃ³n no bloqueante del servidor/tooling local.

El esquema fue verificado directamente mediante migraciones, Tinker y `db:table`.

### 6.8 ERP Core post-rebuild

```text
ERP Core                     0.1.0
Autodiscovery                Habilitado
MÃ³dulos                      1
Organizacion                 0.1.0
```

### 6.9 Suite post-rebuild

Resultado:

```text
61 passed
119 assertions
```

La coincidencia exacta entre la suite anterior y posterior al rebuild demostrÃ³ la estabilidad de la baseline.

Documento asociado:

`S1.4.1.4_VALIDACION_BASELINE_FUNCIONAL.md`

---

# 7. Arquitectura baseline resultante

La baseline de S1.4.1 puede resumirse como:

```text
Laravel 13
â”‚
â”œâ”€â”€ Identidad mÃ­nima
â”‚   â”œâ”€â”€ users
â”‚   â”œâ”€â”€ roles
â”‚   â””â”€â”€ password_reset_tokens
â”‚
â”œâ”€â”€ ERP Core
â”‚   â”œâ”€â”€ Builders
â”‚   â”œâ”€â”€ Contracts
â”‚   â”œâ”€â”€ FileSystem
â”‚   â”œâ”€â”€ Kernel
â”‚   â”œâ”€â”€ Providers
â”‚   â”œâ”€â”€ Template
â”‚   â”œâ”€â”€ Templates
â”‚   â””â”€â”€ ValueObjects
â”‚
â”œâ”€â”€ MÃ³dulos
â”‚   â””â”€â”€ Organizacion 0.1.0
â”‚
â””â”€â”€ Testing
    â”œâ”€â”€ Feature
    â”œâ”€â”€ Unit/Core
    â””â”€â”€ Fixtures
```

La baseline no contiene todavÃ­a la reconstrucciÃ³n funcional completa de identidad, autorizaciÃ³n o dominio empresarial.

Es deliberadamente mÃ­nima.

---

# 8. Estado canÃ³nico de la baseline

```text
Framework                       Laravel 13.20.0
PHP CLI                         8.4.5
Composer                        2.10.2

Migraciones baseline            4
Roles baseline                  2
users                           14 columnas
password_reset_tokens           presente

ERP Core                        0.1.0
Autodiscovery                   habilitado
MÃ³dulos descubiertos            1
Organizacion                    0.1.0

Suite                           61 passed
Assertions                      119

Registro pÃºblico                ausente intencionalmente
Archivos vacÃ­os app/Core        0
.gitkeep app/Core               0
Archivos vacÃ­os tests           0
assertTrue(true) residual       0
```

---

# 9. Matriz de cierre

| Subfase | Resultado |
|---|---|
| S1.4.1.1 â€” Integridad estructural ERP Core | CERRADO / VALIDADO |
| S1.4.1.2 â€” NormalizaciÃ³n estructural ERP Core | CERRADO / VALIDADO |
| S1.4.1.3 â€” NormalizaciÃ³n suite de pruebas | CERRADO / VALIDADO |
| S1.4.1.4 â€” ValidaciÃ³n baseline funcional | CERRADO / VALIDADO |

Resultado global:

```text
S1.4.1 â€” BASELINE FUNCIONAL E INTEGRIDAD ARQUITECTÃ“NICA

CERRADO / VALIDADO
```

---

# 10. Incidencias no bloqueantes

Durante la ejecuciÃ³n se identificaron incidencias que no representan defectos del cÃ³digo baseline:

### Terminal / codepage

Algunos caracteres UTF-8 se visualizaron incorrectamente en la consola.

La auditorÃ­a de archivos confirmÃ³ UTF-8 vÃ¡lido y ausencia de BOM/mojibake en fuente.

### PowerShell + Tinker

Algunas ejecuciones `tinker --execute` produjeron errores por quoting del shell.

Las verificaciones fueron repetidas mediante Tinker interactivo.

### db:show

La instalaciÃ³n local de MySQL/MariaDB no proporcionÃ³ la estructura de `performance_schema` esperada por el comando.

El esquema fue validado mediante mecanismos alternativos.

### CRLF â†’ LF

`git diff --check` mostrÃ³ advertencias de normalizaciÃ³n de finales de lÃ­nea en archivos concretos, sin errores bloqueantes de whitespace.

---

# 11. Criterios globales de aceptaciÃ³n

| Criterio | Estado |
|---|---|
| ERP Core identificado y saneado | VALIDADO |
| Arquitectura Core normalizada | VALIDADO |
| Responsabilidades CLI/Core separadas | VALIDADO |
| Autodiscovery operativo | VALIDADO |
| Templates canÃ³nicos definidos | VALIDADO |
| Suite sin boilerplate ficticio | VALIDADO |
| Suite sin archivos vacÃ­os | VALIDADO |
| SQLite de testing operativo | VALIDADO |
| Password reset reproducible | VALIDADO |
| Baseline DB reconstruible desde cero | VALIDADO |
| Roles mÃ­nimos reproducibles | VALIDADO |
| Foreign keys e Ã­ndices validados | VALIDADO |
| ERP operativo despuÃ©s del rebuild | VALIDADO |
| Suite estable despuÃ©s del rebuild | 61 / 119 |
| Registro pÃºblico permanece eliminado | VALIDADO |
| Incidencias locales aisladas del cÃ³digo | VALIDADO |

---

# 12. DocumentaciÃ³n del bloque

S1.4.1 queda respaldado por:

```text
docs/RECONSTRUCCION/
â”œâ”€â”€ S1.4.1.1_INTEGRIDAD_ESTRUCTURAL_ERP_CORE.md
â”œâ”€â”€ S1.4.1.2_NORMALIZACION_ESTRUCTURAL_ERP_CORE.md
â”œâ”€â”€ S1.4.1.3_NORMALIZACION_SUITE_DE_PRUEBAS.md
â”œâ”€â”€ S1.4.1.4_VALIDACION_BASELINE_FUNCIONAL.md
â””â”€â”€ S1.4.1_BASELINE_FUNCIONAL_E_INTEGRIDAD_ARQUITECTONICA.md
```

Los documentos individuales contienen la evidencia y decisiones de cada subfase.

El presente documento constituye el cierre maestro y la referencia canÃ³nica del bloque S1.4.1.

---

# 13. PolÃ­tica de versionado del cierre

Antes de crear el commit de cierre debe revisarse el diff acumulado del repositorio.

No debe utilizarse `git add .` de forma automÃ¡tica.

La secuencia de versionado deberÃ¡:

1. revisar `git diff --stat`;
2. revisar `git diff --name-status`;
3. inspeccionar los archivos modificados relevantes;
4. comprobar que la documentaciÃ³n estÃ© bajo `docs/RECONSTRUCCION`;
5. verificar `git status --short`;
6. decidir la estrategia de staging;
7. ejecutar nuevamente las validaciones necesarias;
8. crear el commit de cierre;
9. etiquetar la baseline si corresponde;
10. hacer push despuÃ©s de verificar commit y tag.

El versionado debe representar fielmente el cierre de S1.4.1 y no mezclar trabajo futuro de S1.4.2.

---

# 14. PrÃ³xima fase

Una vez documentado, revisado y versionado S1.4.1, el siguiente bloque es:

```text
S1.4.2 â€” Identidad, roles y autorizaciÃ³n
```

S1.4.2 deberÃ¡ construirse sobre esta baseline sin reintroducir dependencias o estructuras eliminadas durante el saneamiento.

---

# 15. Cierre formal

Las cuatro subfases de S1.4.1 fueron completadas y validadas.

La aplicaciÃ³n dispone ahora de:

- una baseline reconstruible;
- un ERP Core funcional y normalizado;
- una arquitectura modular verificable;
- una identidad mÃ­nima persistente;
- migraciones y seeders reproducibles;
- una suite de pruebas significativa;
- un mÃ³dulo `Organizacion` detectable;
- una separaciÃ³n clara entre cÃ³digo, tests y configuraciÃ³n local.

Resultado:

```text
S1.4.1.1   CERRADO / VALIDADO
S1.4.1.2   CERRADO / VALIDADO
S1.4.1.3   CERRADO / VALIDADO
S1.4.1.4   CERRADO / VALIDADO

S1.4.1 â€” BASELINE FUNCIONAL E INTEGRIDAD ARQUITECTÃ“NICA
ESTADO GLOBAL: CERRADO / VALIDADO
```

La baseline queda preparada para su revisiÃ³n de diff, versionado y posterior inicio de **S1.4.2 â€” Identidad, roles y autorizaciÃ³n**.
