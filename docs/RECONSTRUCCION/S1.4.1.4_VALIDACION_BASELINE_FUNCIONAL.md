# S1.4.1.4 â€” ValidaciÃ³n del baseline funcional

**Fase:** S1.4 â€” ReconstrucciÃ³n funcional sobre baseline saneada
**Subfase:** S1.4.1 â€” Baseline funcional e integridad arquitectÃ³nica
**Componente:** Baseline funcional integral
**Estado:** CERRADO / VALIDADO

## 1. Objetivo

Validar de manera integral que la baseline saneada y normalizada pueda reconstruirse desde cero y permanecer operativa.

La subfase comprueba conjuntamente:

- entorno CLI;
- migraciones;
- seeders;
- esquema de base de datos;
- identidad mÃ­nima;
- relaciones e Ã­ndices;
- ERP Core;
- descubrimiento modular;
- suite completa de pruebas.

S1.4.1.4 no introduce funcionalidad de negocio. Su propÃ³sito es demostrar que el estado resultante de S1.4.1 constituye una base reproducible y tÃ©cnicamente consistente para continuar con S1.4.2.

## 2. Precondiciones

La validaciÃ³n se realizÃ³ despuÃ©s del cierre de:

- **S1.4.1.1 â€” Integridad estructural del ERP Core**
- **S1.4.1.2 â€” NormalizaciÃ³n estructural del ERP Core**
- **S1.4.1.3 â€” NormalizaciÃ³n de la suite de pruebas**

La suite normalizada recibida como criterio de aceptaciÃ³n fue:

```text
61 passed
119 assertions
```

## 3. Entorno validado

La validaciÃ³n se realizÃ³ sobre la rama:

```text
rebuild/s1.4-base-funcional
```

Entorno observado:

```text
PHP CLI        8.4.5
php.ini        C:\php84\php.ini
Laravel        13.20.0
Composer       2.10.2
APP_ENV        local
APP_DEBUG      habilitado
APP_URL        http://127.0.0.1:8000
Locale         es
```

La conexiÃ³n de base de datos utilizada por la aplicaciÃ³n fue:

```text
Driver         mysql
Database       sembrando_vida
Host           127.0.0.1
Port           3307
```

## 4. Migraciones baseline

El baseline quedÃ³ definido por cuatro migraciones:

```text
0001_01_01_000000_create_roles_table.php
0001_01_01_000001_create_users_table.php
0001_01_01_000002_create_cache_table.php
0001_01_01_000003_create_jobs_table.php
```

Antes de la reconstrucciÃ³n controlada, `php artisan migrate:status` confirmÃ³ que las cuatro estaban ejecutadas.

## 5. ReconstrucciÃ³n controlada desde cero

Se ejecutÃ³:

```powershell
php artisan migrate:fresh --seed
```

Resultado:

- todas las tablas existentes fueron eliminadas;
- se recreÃ³ la tabla de migraciones;
- se ejecutaron correctamente las cuatro migraciones baseline;
- se ejecutÃ³ `Database\Seeders\RolesSeeder`;
- el proceso terminÃ³ sin error de migraciÃ³n ni de seeding.

Posteriormente:

```powershell
php artisan migrate:status
```

confirmÃ³ las cuatro migraciones como:

```text
[1] Ran
```

Esto valida que el baseline puede reconstruirse a partir del cÃ³digo versionable del proyecto.

## 6. Roles baseline

`RolesSeeder` reconstruyÃ³ correctamente los roles mÃ­nimos:

| id | name |
|---:|---|
| 1 | Administrador |
| 2 | Usuario |

No se restauraron roles heredados de dominios eliminados durante el saneamiento.

## 7. Tablas resultantes

La inspecciÃ³n directa del esquema confirmÃ³ las tablas:

```text
cache
cache_locks
failed_jobs
job_batches
jobs
migrations
password_reset_tokens
roles
users
```

Estas tablas corresponden a la baseline mÃ­nima de Laravel, identidad y soporte operativo requerido.

## 8. Tabla users

La tabla `users` fue validada con 14 columnas:

```text
id
name
apellido_paterno
apellido_materno
curp
email
celular
email_verified_at
password
activo
role_id
remember_token
created_at
updated_at
```

La inspecciÃ³n mediante Artisan confirmÃ³ ademÃ¡s:

```text
Engine       InnoDB
Collation    utf8mb4_unicode_ci
```

CaracterÃ­sticas relevantes:

- `id`: bigint unsigned, autoincrement;
- `name`: varchar(150);
- `apellido_paterno`: nullable;
- `apellido_materno`: nullable;
- `curp`: nullable, varchar(18);
- `email`: varchar(255);
- `celular`: nullable, varchar(20);
- `email_verified_at`: nullable timestamp;
- `password`: varchar(255);
- `activo`: tinyint(1), default 1;
- `role_id`: nullable bigint unsigned;
- `remember_token`: nullable varchar(100);
- timestamps de creaciÃ³n y actualizaciÃ³n.

## 9. Ãndices y unicidad

Se validaron los Ã­ndices principales de `users`:

```text
PRIMARY              id
UNIQUE               email
UNIQUE               curp
UNIQUE               celular
INDEX                role_id
```

Esto confirma que la baseline conserva las restricciones mÃ­nimas definidas para identidad.

## 10. RelaciÃ³n users.role_id â†’ roles.id

La inspecciÃ³n del esquema confirmÃ³:

```text
users.role_id
    â†“
roles.id
```

La foreign key quedÃ³ configurada con:

```text
ON UPDATE    RESTRICT
ON DELETE    SET NULL
```

La eliminaciÃ³n de un rol no elimina usuarios asociados; su `role_id` se establece en `NULL`.

Esto es coherente con la definiciÃ³n `nullOnDelete()` de la migraciÃ³n.

## 11. password_reset_tokens

La tabla requerida por el password broker quedÃ³ presente despuÃ©s de `migrate:fresh --seed`.

Columnas:

```text
email
token
created_at
```

CaracterÃ­sticas principales:

- `email` funciona como clave primaria;
- `token` almacena el token de recuperaciÃ³n;
- `created_at` es nullable.

Su existencia confirma que la correcciÃ³n realizada en S1.4.1.3 forma parte reproducible del baseline.

## 12. NormalizaciÃ³n de PHP intl

Durante las herramientas de inspecciÃ³n de base de datos se detectÃ³ que la extensiÃ³n `intl` no estaba habilitada en el PHP CLI.

Se verificÃ³:

```text
Loaded Configuration File:
C:\php84\php.ini
```

El archivo de extensiÃ³n existÃ­a:

```text
C:\php84\ext\php_intl.dll
```

y la configuraciÃ³n contenÃ­a:

```text
;extension=intl
```

Se habilitÃ³:

```text
extension=intl
```

La validaciÃ³n posterior confirmÃ³:

```text
Internationalization support    enabled
ICU version                     75.1
ICU Data version                75.1
ICU TZData version              2024a
Unicode version                 15.1
```

DespuÃ©s de habilitar `intl`, comandos como:

```powershell
php artisan db:table users
```

funcionaron correctamente.

Esta modificaciÃ³n pertenece al entorno local `C:\php84\php.ini` y **no forma parte del repositorio Git**.

## 13. LimitaciÃ³n local de db:show

La ejecuciÃ³n de:

```powershell
php artisan db:show
```

encontrÃ³ una limitaciÃ³n del servidor MySQL/MariaDB local:

```text
performance_schema.session_status doesn't exist
```

El problema se clasificÃ³ como una limitaciÃ³n de compatibilidad del servidor/herramienta local y no como defecto del baseline.

No fue bloqueante porque el esquema se validÃ³ directamente mediante:

- migraciones;
- Tinker;
- `db:table`;
- consultas de tablas, columnas, Ã­ndices y foreign keys.

Por lo tanto, `db:show` no se utilizÃ³ como criterio Ãºnico de aceptaciÃ³n.

## 14. ERP Core post-rebuild

DespuÃ©s de reconstruir la base se ejecutÃ³:

```powershell
php artisan erp:status
```

Resultado:

```text
ERP Core                     0.1.0
Descubrimiento automÃ¡tico    Habilitado
MÃ³dulos descubiertos         1
MÃ³dulo                       Organizacion
VersiÃ³n                      0.1.0
Provider                     App\Modules\Organizacion\Providers\OrganizacionServiceProvider
```

La reconstrucciÃ³n de base de datos no afectÃ³ el boot del ERP ni el descubrimiento modular.

## 15. Suite completa post-rebuild

DespuÃ©s de `migrate:fresh --seed` se ejecutÃ³ nuevamente la suite completa.

Resultado:

```text
Tests:       61 passed
Assertions:  119
Duration:    4.24s
```

La cifra coincide con el baseline de pruebas establecido al cierre de S1.4.1.3:

```text
Antes del rebuild     61 passed / 119 assertions
DespuÃ©s del rebuild   61 passed / 119 assertions
```

Esto constituye la evidencia principal de estabilidad funcional de la reconstrucciÃ³n.

## 16. Composer y autoload

La regeneraciÃ³n de autoload se completÃ³ correctamente:

```text
Generated optimized autoload files containing 6728 classes
```

La diferencia respecto de conteos previos es coherente con la eliminaciÃ³n controlada de archivos/tests residuales durante la normalizaciÃ³n y no representÃ³ una falla de autoload.

## 17. Rutas y registro pÃºblico

La baseline mantenÃ­a 21 rutas en la validaciÃ³n previa al rebuild.

Se confirmÃ³ que el registro pÃºblico continÃºa ausente de forma intencional.

Se conservaron los flujos base necesarios para:

- raÃ­z;
- dashboard;
- autenticaciÃ³n;
- recuperaciÃ³n/actualizaciÃ³n de contraseÃ±a;
- verificaciÃ³n;
- perfil.

No se reintrodujo funcionalidad eliminada para satisfacer pruebas antiguas.

## 18. Observaciones de tooling

Durante algunas verificaciones con:

```powershell
php artisan tinker --execute='...'
```

se produjeron errores de parsing derivados del quoting entre PowerShell y Tinker.

Las mismas comprobaciones se realizaron posteriormente mediante Tinker interactivo y confirmaron correctamente tablas, columnas, roles y foreign keys.

Estos errores se clasificaron como problemas de invocaciÃ³n de shell y no como defectos de Laravel, PHP o del baseline.

## 19. Criterios de aceptaciÃ³n

| Criterio | Resultado |
|---|---|
| PHP CLI 8.4.5 | VALIDADO |
| `C:\php84\php.ini` identificado | VALIDADO |
| PHP `intl` | HABILITADO / VALIDADO |
| Laravel 13.20.0 | VALIDADO |
| Composer 2.10.2 | VALIDADO |
| Entorno local | VALIDADO |
| ConexiÃ³n MySQL baseline | VALIDADO |
| `migrate:fresh --seed` | VALIDADO |
| Cuatro migraciones baseline | VALIDADO |
| `RolesSeeder` | VALIDADO |
| Rol Administrador | VALIDADO |
| Rol Usuario | VALIDADO |
| Tablas baseline | VALIDADO |
| `users` | 14 COLUMNAS / VALIDADO |
| `password_reset_tokens` | VALIDADO |
| `users.role_id â†’ roles.id` | VALIDADO |
| `ON DELETE SET NULL` | VALIDADO |
| Ãndices Ãºnicos | VALIDADO |
| ERP Core | OPERATIVO |
| ERP Core version | 0.1.0 |
| Autodiscovery | HABILITADO |
| Organizacion | 0.1.0 / VALIDADO |
| Suite post-rebuild | 61 PASSED |
| Assertions | 119 |
| `db:show` | LIMITACIÃ“N LOCAL NO BLOQUEANTE |

## 20. Estado formal de S1.4.1.4

```text
S1.4.1.4 â€” ValidaciÃ³n del baseline funcional

PHP CLI / php.ini              VALIDADO
PHP intl                       HABILITADO / VALIDADO
Laravel 13.20.0                VALIDADO
Entorno local                  VALIDADO

migrate:fresh --seed           VALIDADO
Migraciones baseline           VALIDADO
RolesSeeder                    VALIDADO
Administrador / Usuario        VALIDADO

Tablas baseline                VALIDADO
users                          14 COLUMNAS / VALIDADO
password_reset_tokens          VALIDADO
users.role_id â†’ roles.id       VALIDADO
ON DELETE                      SET NULL
Ãndices unique                 VALIDADO

ERP Core                       OPERATIVO
Organizacion 0.1.0             VALIDADO
Suite post-rebuild             61 PASSED
Assertions                     119

S1.4.1.4                       CERRADO / VALIDADO
```

## 21. Consecuencia sobre S1.4.1

Con esta validaciÃ³n quedan cerradas las cuatro subfases:

```text
S1.4.1 â€” Baseline funcional e integridad arquitectÃ³nica

S1.4.1.1  Integridad estructural ERP Core       CERRADO / VALIDADO
S1.4.1.2  NormalizaciÃ³n estructural ERP Core    CERRADO / VALIDADO
S1.4.1.3  NormalizaciÃ³n suite de pruebas        CERRADO / VALIDADO
S1.4.1.4  ValidaciÃ³n del baseline funcional     CERRADO / VALIDADO

ESTADO GLOBAL S1.4.1: CERRADO / VALIDADO
```

## 22. Cierre

La baseline puede reconstruirse desde cero mediante migraciones y seeders, conserva su esquema mÃ­nimo de identidad, mantiene operativo ERP Core y reproduce exactamente el resultado esperado de la suite normalizada.

Las incidencias encontradas durante la validaciÃ³n correspondieron a configuraciÃ³n del entorno CLI o tooling local y fueron aisladas de los defectos del cÃ³digo.

```text
S1.4.1.4 â€” ValidaciÃ³n del baseline funcional
ESTADO: CERRADO / VALIDADO

S1.4.1 â€” Baseline funcional e integridad arquitectÃ³nica
ESTADO GLOBAL: CERRADO / VALIDADO
```

El repositorio queda tÃ©cnicamente preparado para documentar el cierre maestro de S1.4.1, revisar el diff acumulado y versionar la baseline antes de iniciar **S1.4.2 â€” Identidad, roles y autorizaciÃ³n**.
